[package]
name = "bogk-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
bogk-core = { path = "../bogk-core" }
multiboot2 = "0.24.1"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
