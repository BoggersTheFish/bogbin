[package]
name = "bogk-kernel"
version = "0.1.0"
edition = "2021"

[features]
default = []
baremetal = []

[dependencies]
bogk-boot-fb = { path = "../bogk-boot-fb" }
bogk-core = { path = "../bogk-core" }
