[package]
name = "bogk-kernel"
version = "0.1.0"
edition = "2021"

[features]
default = []
baremetal = []

[dependencies]
bogk-core = { path = "../bogk-core" }

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
